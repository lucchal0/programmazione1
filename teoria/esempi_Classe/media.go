package main 

import "fmt"

func media(num1 float64, num2 float64)(res float64){    // func nome (input) (output) {}
	res = (num1 + num2) / 2.0
    // si può inseire il return per riportare un variabile al main
}

func main(){

var risultato float64

	risultsto = media(3.1, 7.2)


	}
